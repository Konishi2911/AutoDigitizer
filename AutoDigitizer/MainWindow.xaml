<Window x:Class="AutoDigitizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AutoDigitizer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Height="434" Margin="10,10,10,10">
            <Expander Header="Open Chart" FontSize="16">
                <Button Content="Reference..." FontSize="12" Command="{Binding OFDCommand}"/>
            </Expander>
            <Expander Header="Set Axis" FontSize="16">
                <TextBlock Text="Set Axis" TextWrapping="Wrap"/>
            </Expander>
            <Expander Header="Extract Line" FontSize="16">
                <TextBlock Text="Extract Line" TextWrapping="Wrap"/>
            </Expander>

        </StackPanel>
        <Image Grid.Column="1" Margin="10,10,10,10" Source="{Binding ImageSource}">
        </Image>
    </Grid>
</Window>
